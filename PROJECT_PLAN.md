# InsightTrace 项目规划与决策记录

> 本文档用于持续记录 InsightTrace 的产品规划、范围边界、技术决策、实施进度和方案变更。
>
> 当前状态：M5 进行中，演示场景、多轮脚本、接口与运维文档已完成
> 当前版本：`MVP v1` 规划草案
> 首次建立：2026-09-16
> 最近更新：2026-09-18

## 1. 项目概述

InsightTrace 是一个面向经营分析场景的多轮归因分析系统。

用户可以围绕一个业务问题持续追问，系统结合业务数据库、用户上传的资料和历史会话上下文执行分析，实时展示分析过程，并最终输出带有证据依据的结构化归因结论和可下载报告。

项目首版强调：

- 分析过程可观察；
- 分析结论可追溯；
- 演示链路可复现；
- 项目能够独立部署运行；
- 在有限时间内优先完成完整业务闭环。

## 2. 产品目标

### 2.1 核心目标

1. 支持用户通过多轮会话提出、补充和细化经营分析问题。
2. 支持分析任务读取业务数据库和用户上传的资料。
3. 实时展示任务状态、分析步骤、工具执行和异常信息。
4. 生成包含问题、指标、证据、结论和建议的结构化结果。
5. 保存分析上下文、历史消息和分析结果，支持继续追问。
6. 提供至少两个完整业务场景及可直接运行的演示数据。

### 2.2 核心价值

InsightTrace 不仅输出一段分析文字，还需要说明：

- 使用了哪些数据；
- 观察到了哪些指标；
- 哪些证据支持当前判断；
- 结论的可信程度如何；
- 当前还缺少哪些信息；
- 下一步可以采取哪些行动。

### 2.3 成功标准

项目成功需要同时满足：

- 新环境可以按照说明快速启动；
- 不依赖真实企业系统也能完成全部演示；
- 两个示例场景均能从登录完整运行到报告下载；
- 每项主要结论至少关联一项可查看的证据；
- 多轮追问能够继承之前的对话和分析结果；
- 页面刷新后仍能查看历史会话、消息和结果；
- 失败任务具有明确状态、步骤和错误原因；
- 系统能阻止危险查询、越权访问和同会话并发任务。

## 3. 目标用户与角色

### 3.1 分析用户

可以：

- 登录系统；
- 创建、重命名、切换和删除自己的会话；
- 上传和管理会话附件；
- 发起分析、查看实时过程和取消任务；
- 查看历史消息及分析结果；
- 复制或下载分析报告。

### 3.2 系统管理员

除分析用户能力外，还可以：

- 查看系统运行状态和任务日志；
- 查看并重新加载系统配置；
- 管理演示数据源连接；
- 管理允许启用的分析工具和功能开关。

首版不建设复杂的组织、部门、岗位和数据权限体系，仅实现满足演示需要的用户角色隔离。

## 4. MVP v1 功能范围

### 4.1 认证与权限

- 授权登录入口；
- OAuth/OIDC 风格的授权跳转与登录回调；
- 登录态保存和接口鉴权；
- 当前用户识别；
- 分析用户和系统管理员两种角色；
- 用户资源归属校验。

首版建议提供内置演示认证服务或模拟认证中心，并预留真实认证中心配置，避免验收依赖外部系统。

### 4.2 会话与消息

- 创建会话；
- 会话列表；
- 会话重命名；
- 会话切换；
- 会话删除；
- 历史消息回放；
- 多轮上下文延续；
- 长会话上下文摘要。

会话状态至少包括：

- `active`
- `archived`
- `deleted`

### 4.3 附件管理

首版支持：

- CSV；
- XLSX；
- JSON；
- TXT。

附件能力包括：

- 上传进度和结果展示；
- 文件类型、大小和路径校验；
- 文件解析状态展示；
- 附件列表；
- 下载；
- 删除；
- 分析任务按会话使用附件内容。

DOCX、扫描 PDF、图片 OCR 暂不纳入 MVP。

### 4.4 分析任务

- 一条用户消息对应一个分析任务；
- 同一个会话同一时间只允许一个运行中的任务；
- 支持任务排队、运行、成功、失败和取消状态；
- 支持查看当前分析步骤；
- 支持用户取消运行中的任务；
- 保存任务输入、状态、时间、错误和执行日志；
- 任务失败不得丢失已经保存的用户消息。

任务状态至少包括：

- `queued`
- `running`
- `success`
- `failed`
- `cancelled`

### 4.5 实时通信

通过 WebSocket 返回分析过程，至少支持：

- `message_start`
- `message_delta`
- `tool_start`
- `tool_finish`
- `task_status`
- `result_ready`
- `error`
- `done`

WebSocket 使用短时、一次性临时令牌鉴权。

### 4.6 分析工具

首版分析工具包括：

- 受控的只读 SQL 查询；
- CSV、XLSX、JSON 和 TXT 内容读取；
- 附件文本检索；
- 表格统计和指标计算；
- 分析中间结果文件生成；
- Markdown 报告生成。

项目要求中的“命令执行”仅解释为后端调用预定义、参数受控的内部工具，不向用户或模型开放任意 Shell。

### 4.7 结构化结果

每次成功分析至少输出六部分：

1. `问题定义`：本轮实际回答的业务问题；
2. `关键指标`：指标名称、数值、单位和统计周期；
3. `证据列表`：来源类型、来源名称、证据内容、关联指标和可信度；
4. `归因结论`：主要原因、次要原因及影响范围；
5. `待补充数据`：当前缺失或不足的数据；
6. `下一步建议`：至少两条可执行建议。

### 4.8 结果管理

- 结果结构化入库；
- 在工作台右侧展示最新结果；
- 查看历史任务结果；
- 复制结果文本；
- 导出 Markdown 报告；
- 下载已生成的结果文件。

PDF 导出暂不作为 MVP 必须项。

### 4.9 配置与日志

- 查看允许公开的系统配置；
- 通过管理接口重新加载配置；
- 查看任务运行日志；
- 查看工具执行摘要；
- 查看错误信息；
- 对密钥、令牌、密码和敏感参数进行脱敏。

## 5. 示例业务场景

MVP v1 选择以下两个场景完成全链路演示。

### 5.1 商品目录优化

建议数据表：

- 商品表；
- 类目表；
- 搜索曝光表；
- 点击表；
- 转化表。

典型问题：

- 为什么某类目本月转化率下降？
- 哪些商品获得了较高曝光但点击表现较差？
- 搜索词、商品信息或类目结构可能造成了哪些损耗？
- 优先应该优化哪些商品或类目？

### 5.2 客户行为分析

建议数据表：

- 用户表；
- 访问事件表；
- 加购事件表；
- 下单事件表。

典型问题：

- 为什么本周访问到下单的转化率下降？
- 损耗主要发生在哪个漏斗环节？
- 哪些渠道、地区或用户群体受到的影响最大？
- 是否存在访问增加但高意向行为下降的情况？

两个场景都需要准备：

- 数据库初始化脚本；
- 可读的字段说明；
- 足以产生明显结论的演示数据；
- 首轮分析问题；
- 至少一次基于上一轮结果的追问；
- 完整的预期结果和操作步骤。

## 6. 明确不在 MVP 范围内

以下能力暂不实现：

- 通用拖拽式 BI 和自由搭建仪表盘；
- 对接真实企业 ERP、CRM 或数据仓库；
- 用户自由配置任意数据库连接；
- 用户直接输入任意 SQL；
- 任意系统命令或 Shell 执行；
- 自动修改业务数据库或业务配置；
- 多智能体编排平台；
- 向量知识库和复杂 RAG 平台；
- DOCX、扫描 PDF 和图片 OCR；
- 多租户计费系统；
- 复杂组织架构和行列级数据权限；
- 移动端专项适配；
- 模型训练和微调；
- 根据分析建议自动执行经营动作；
- 高可用集群、自动扩缩容和大规模并发优化。

以上内容只有在 MVP 完整闭环已经稳定后，才进入后续版本评估。

## 7. 技术方案草案

### 7.1 推荐技术栈

| 层级 | 推荐方案 | 说明 |
| --- | --- | --- |
| 前端 | React + TypeScript + Vite | 构建聊天工作台和管理页面 |
| 后端 | FastAPI + Pydantic | 提供 REST、WebSocket 和任务编排 |
| ORM | SQLAlchemy | 管理业务数据表和事务 |
| 数据库 | PostgreSQL | 保存用户、会话、任务、结果及演示数据 |
| 数据分析 | Python 标准库 + openpyxl；M4 再接入 Pandas | 当前负责附件解析，后续处理统计分析 |
| 实时通信 | WebSocket | 推送分析状态、文本和工具事件 |
| 模型接口 | OpenAI-compatible API | 通过配置切换模型供应方 |
| 部署 | Docker Compose | 支持一键启动完整演示环境 |
| 报告 | Markdown | MVP 的主要导出格式 |

### 7.2 模型可用性策略

系统应支持两种运行模式：

1. **模型模式**：配置兼容接口和密钥后，由模型完成问题理解、工具选择和结果组织；
2. **演示模式**：不依赖外部密钥，使用预设问题识别、统计分析和报告模板跑通验收链路。

演示模式用于保证项目在网络或模型服务不可用时仍可复现，不应伪装成完整智能分析能力。

### 7.3 初步模块划分

后端模块：

- `auth`：认证接入和当前用户识别；
- `conversations`：会话管理；
- `messages`：消息保存和历史查询；
- `attachments`：文件保存、解析和下载；
- `tasks`：任务创建、状态流转和取消；
- `realtime`：WebSocket 连接和事件推送；
- `analysis`：上下文组装、工具调度和结果生成；
- `tools`：只读查询、文件分析和报告生成；
- `results`：结构化结果保存和导出；
- `configs`：配置读取和热更新；
- `logs`：任务日志和错误记录。

前端模块：

- 登录入口和回调；
- 会话侧栏；
- 聊天消息区；
- 问题输入区；
- 附件侧栏；
- 实时任务区；
- 结构化结果区；
- 管理员配置和日志页。

## 8. 数据与文件边界

### 8.1 文件目录

- 附件：`uploads/{user_id}/{conversation_id}/`
- 导出结果：`exports/{user_id}/{conversation_id}/`
- 临时文件：`workspace/{user_id}/{conversation_id}/`

删除会话时，需要同步删除该会话的消息、附件记录、任务记录、结果记录、上下文摘要以及上述文件目录。

### 8.2 SQL 安全边界

- 只允许执行 `SELECT` 或只读 CTE；
- 禁止多语句查询；
- 禁止 DDL、DML 和数据库管理语句；
- 限制可访问的 schema 和表；
- 设置执行超时；
- 设置最大返回行数；
- 使用只读数据库账号；
- 保存脱敏后的查询和执行摘要；
- 数据库层只读权限是最终保护，不能只依赖文本检查。

### 8.3 文件安全边界

- 限制允许上传的扩展名；
- 限制单文件大小和会话总容量；
- 服务端生成实际存储名称；
- 禁止通过文件名控制保存路径；
- 下载和删除操作必须校验文件归属；
- 业务接口不得接受任意本地绝对路径；
- 日志不得输出文件中的敏感完整内容。

## 9. 页面范围

### 9.1 登录入口页

- 产品名称和简要介绍；
- 登录按钮；
- 登录失败提示。

### 9.2 登录回调页

- 接收授权回调参数；
- 换取或建立登录态；
- 显示处理中或错误状态；
- 成功后跳转聊天工作台。

### 9.3 聊天工作台

- 左侧：会话列表、新建、重命名和删除；
- 中间：消息历史、流式回答、输入框和取消按钮；
- 右侧：任务过程、附件和结构化分析结果；
- 支持旧会话回放和继续追问。

具体布局可以在原型阶段调整，但上述功能区域必须保留。

### 9.4 管理页面

- 系统状态；
- 可公开配置；
- 配置重新加载；
- 任务日志与错误详情；
- 演示数据源状态。

## 10. 实施里程碑

### M0：方案确认

- [x] 确定项目定位；
- [x] 提出 MVP 范围建议；
- [x] 提出明确的非目标范围；
- [x] 选择两个推荐演示场景；
- [x] 建立项目规划文档；
- [x] 最终确认技术栈；
- [x] 最终确认认证实现方式；
- [x] 最终确认模型接入与演示模式策略。

### M1：项目基础骨架

- [x] 建立前后端目录结构；
- [x] 建立 Docker Compose；
- [x] 建立 PostgreSQL 初始化脚本；
- [x] 实现基础配置管理；
- [x] 建立核心 SQLAlchemy 数据模型；
- [x] 建立第一版 Alembic 数据库迁移；
- [x] 建立数据库结构和关键约束测试；
- [x] 实现认证和角色识别；
- [x] 建立统一错误响应和日志规范。

### M2：聊天工作台闭环

- [x] 实现会话创建和当前用户会话列表；
- [x] 实现会话重命名、归档和删除；
- [x] 实现消息保存和历史回放；
- [x] 实现附件上传、列表、下载和删除；
- [x] 实现附件内容解析和状态更新；
- [x] 完成聊天工作台主要布局；
- [x] 实现当前会话、消息和附件的用户资源隔离。

### M3：实时分析任务

- [x] 实现任务创建、查询和失败/取消后重试；
- [x] 实现完整任务状态转换和成功结果原子提交；
- [x] 实现运行中任务的后端重启恢复；
- [x] 实现分析任务执行器；
- [x] 实现单会话并发限制；
- [x] 实现排队任务取消和运行任务协作式取消请求；
- [x] 实现一次性 WebSocket 临时令牌；
- [x] 实现连接、任务状态、错误和结束实时事件；
- [x] 实现执行器文本、工具和结果实时事件；
- [x] 实现基础任务日志；
- [x] 实现执行器步骤和工具日志。

### M4：归因分析能力

- [x] 实现只读 SQL 工具；
- [x] 实现附件读取与文本检索；
- [x] 实现指标计算；
- [x] 实现上下文组装和摘要；
- [x] 实现六部分结构化结果；
- [x] 实现 Markdown 报告导出。

### M5：演示与验收

- [x] 完成商品目录优化演示数据；
- [x] 完成客户行为分析演示数据；
- [x] 完成两个场景的多轮分析脚本；
- [x] 完成接口说明；
- [x] 完成部署和操作说明；
- [ ] 完成功能测试和安全边界测试；
- [ ] 整理测试截图和最终交付目录。

## 11. 验收清单

### 11.1 功能验收

- [ ] 能通过授权登录进入系统；
- [ ] 能创建、重命名、切换和删除会话；
- [x] 能上传、查看、下载和删除附件；
- [ ] 能发送问题并创建分析任务；
- [ ] 能实时看到任务状态、工具过程和增量文本；
- [ ] 能取消正在运行的任务；
- [ ] 能查看历史消息并继续追问；
- [ ] 能看到六部分结构化结果；
- [ ] 能导出并重新下载报告；
- [ ] 管理员能重新加载配置并查看日志；
- [ ] 两个业务场景都能完整演示。

### 11.2 边界验收

- [ ] 非法或写入型 SQL 被拒绝；
- [ ] 超时或超量查询被中止；
- [ ] 用户不能访问其他用户的资源；
- [ ] 非法文件类型和超大文件被拒绝；
- [ ] 文件路径穿越请求被拒绝；
- [ ] 同一会话不能同时运行两个任务；
- [ ] 失效或重复使用的 WebSocket 令牌被拒绝；
- [ ] 删除会话后关联记录和文件被清理；
- [ ] 日志不暴露密码、令牌或模型密钥。

## 12. 风险与应对

| 风险 | 影响 | 当前应对方案 |
| --- | --- | --- |
| 外部认证中心不可用 | 无法完成登录演示 | 提供内置演示认证服务，并保留外部接入配置 |
| 模型接口或网络不可用 | 无法完成分析链路 | 提供确定性的演示分析模式 |
| 模型生成危险 SQL | 数据安全和任务失败 | 只读账号、SQL 校验、表白名单、超时和行数限制共同防护 |
| 演示数据缺少明显异常 | 分析结论不稳定 | 预先设计带有可验证原因链的数据集 |
| 功能范围持续扩大 | 延误核心闭环 | 以本文档的 MVP 和非目标范围作为变更依据 |
| 长连接中断 | 页面状态和服务端不一致 | 保存任务状态，支持重连后通过任务接口恢复 |
| 多轮上下文过长 | 成本和响应时间增加 | 保存阶段性摘要，仅组装必要历史内容 |

## 13. 待确认事项

以下内容尚未成为最终决策：

1. 默认使用的具体 OpenAI-compatible 模型及推荐参数；
2. 单文件大小、会话总容量和查询最大行数的具体限制；
3. 最终提交文件夹的组名、姓名和行业命名信息。

## 14. 决策记录

所有会影响范围、架构、工期或验收的决定均在此记录。状态可使用：`提议`、`已确认`、`已废弃`。

| 编号 | 日期 | 状态 | 决策 | 原因与影响 |
| --- | --- | --- | --- | --- |
| D-001 | 2026-09-16 | 已确认 | 首版定位为可演示、可复现的经营分析归因系统 | 优先完成完整闭环，不建设通用 BI 平台 |
| D-002 | 2026-09-16 | 已确认 | MVP 选择商品目录优化和客户行为分析两个场景 | 两个场景数据结构清晰，适合展示指标归因和多轮追问 |
| D-003 | 2026-09-16 | 已确认 | 不开放任意 SQL 和任意命令执行 | 降低安全风险并控制项目范围 |
| D-004 | 2026-09-16 | 已确认 | 提供无需外部模型密钥的演示模式 | 保证验收环境能够稳定复现完整链路 |
| D-005 | 2026-09-16 | 已确认 | MVP 报告以 Markdown 为强制导出格式 | 降低实现复杂度，PDF 可在后续版本增加 |
| D-006 | 2026-09-16 | 已确认 | 使用 React、TypeScript、FastAPI 和 PostgreSQL，采用 Docker Compose 部署 | 技术生态成熟，适合快速完成前后端和数据分析闭环 |
| D-007 | 2026-09-16 | 已确认 | 认证采用内置模拟 OAuth 流程，并保留真实认证中心适配接口 | 保证演示独立可运行，同时控制后续替换成本 |
| D-008 | 2026-09-16 | 已确认 | 模型通过 OpenAI-compatible 接口接入，并使用环境变量切换配置 | 避免绑定特定模型供应方 |
| D-009 | 2026-09-16 | 已确认 | MVP 提供指标卡、漏斗图和趋势图，不建设通用看板 | 满足经营分析展示需要并控制前端范围 |
| D-010 | 2026-09-16 | 已确认 | 数据查询采用只读账号、SQL 校验和表白名单多层防护 | 避免模型或用户生成的查询修改或越权访问数据 |
| D-011 | 2026-09-16 | 已确认 | MVP 实现会话归档状态及归档操作 | 保留必要的会话生命周期能力，不扩展复杂回收站功能 |
| D-012 | 2026-09-17 | 已确认 | 保留认证所需的 `itsdangerous`、`python-multipart` 依赖及 RESTful 会话路径 | 延续现有认证实现和前后端接口，不再产生兼容性改动 |
| D-013 | 2026-09-17 | 已确认 | 数据库集成测试必须使用名称以 `_test` 结尾的独立数据库 | 避免自动化测试污染本地开发和演示数据 |

## 15. 规划进程日志

### 2026-09-16：项目边界初次讨论

- 检查项目现状：仓库当前只有简要 README，尚未开始实现；
- 明确产品核心为多轮经营归因分析，而非通用 BI；
- 提出 MVP 功能范围和明确的非目标范围；
- 推荐商品目录优化与客户行为分析作为首批演示场景；
- 提出 React、FastAPI、PostgreSQL 和 Docker Compose 技术方案；
- 提出受控只读 SQL、文件路径校验和临时令牌等安全边界；
- 建立本文档，用于后续持续维护规划和决策。

### 2026-09-16：MVP 默认方案确认

- 确认使用 React、TypeScript、FastAPI 和 PostgreSQL；
- 确认使用 Docker Compose 提供一键启动；
- 确认使用内置模拟 OAuth 流程，并保留真实认证中心适配接口；
- 确认通过 OpenAI-compatible 接口接入模型，并提供无密钥演示模式；
- 确认商品目录优化和客户行为分析为 MVP 演示场景；
- 确认 Markdown 为 MVP 强制报告格式；
- 确认前端实现指标卡、漏斗图和趋势图，不建设通用看板；
- 确认只读账号、SQL 校验和表白名单的数据安全策略；
- 确认 MVP 实现会话归档状态及归档操作；
- M0 方案确认完成，项目下一阶段进入系统架构、数据模型和接口契约设计。

### 2026-09-16：技术设计初稿建立

- 建立 `TECHNICAL_DESIGN.md`；
- 采用 React、FastAPI 和 PostgreSQL 的模块化单体架构；
- 明确前端、后端、分析编排、工具、认证和实时通信的模块边界；
- 给出核心数据表、REST API、WebSocket 事件和任务状态机设计；
- 明确模型模式与确定性演示模式使用相同的结构化结果契约；
- 明确分析事务账号与只读数据账号分离；
- 设计首条商品目录优化黄金路径和可验证的原因链；
- 待技术设计确认后进入项目骨架开发。

### 2026-09-16：项目基础骨架建立

- 确认技术设计并进入 M1；
- 建立 React、TypeScript、Vite 前端骨架和基础状态页；
- 建立 FastAPI、SQLAlchemy、Alembic 后端骨架；
- 建立 PostgreSQL 演示 schema 初始化脚本；
- 建立 Docker Compose、环境变量模板和受控存储目录；
- 实现后端存活检查、数据库及文件存储就绪检查；
- 建立前端组件测试和后端健康检查测试入口；
- 前端测试通过，生产构建通过；
- Docker 前后端镜像构建通过；
- PostgreSQL、后端和前端容器启动成功，完整连通性检查通过；
- 将任务重启后的失败、重试和输入快照策略补充进技术设计。

### 2026-09-16：核心数据模型与迁移完成

- 建立用户、会话、消息、附件、任务、结果、摘要、临时令牌、配置和任务日志模型；
- 使用 UUID 主键、带时区时间、JSONB 和明确的状态枚举；
- 建立会话资源级联删除、消息顺序唯一、任务重试来源等关系；
- 通过 PostgreSQL 部分唯一索引保证一个会话只有一个排队中或运行中的任务；
- 建立 `0001_core_schema` Alembic 迁移并在真实 PostgreSQL 容器中执行成功；
- 固定数据库连接搜索路径，避免用户名称与 schema 同名造成 Alembic 重复识别；
- Alembic 一致性检查通过，没有未迁移的模型变更；
- 建立隔离的后端测试镜像；
- Ruff 代码检查通过，后端与数据库结构测试共 5 项通过。

### 2026-09-16：模拟 OAuth 与角色识别完成

- 实现登录发起、模拟授权页面、一次性授权码回调和退出登录；
- 使用签名的 `HttpOnly`、`SameSite=Lax` Cookie 保存短期会话；
- 支持分析用户和系统管理员两种演示身份，并在登录时创建或更新用户记录；
- 实现 `GET /api/me` 当前用户接口和可复用的登录、管理员依赖；
- 前端接入登录状态、身份展示和退出操作；
- 建立统一的认证与权限错误响应结构；
- 修正异步数据库测试连接池的跨事件循环问题；
- Ruff 检查、前端测试及构建通过，后端 6 项测试通过。

### 2026-09-17：M2 首个会话闭环完成

- 实现 `POST /api/conversations`，登录用户可以创建会话；
- 实现 `GET /api/conversations`，查询条件始终包含当前用户 ID；
- 使用请求模型校验并自动清理会话标题前后的空格；
- 前端增加“我的分析会话”区域、创建表单和会话列表；
- 增加用户资源隔离集成测试，确认管理员与分析用户不能看到彼此的会话；
- 后端 7 项测试、前端 2 项测试、Ruff 检查和前端生产构建通过。

### 2026-09-17：用户消息保存与历史回放完成

- 实现会话消息列表和用户消息创建接口；
- 创建消息前锁定所属会话，并按会话生成连续的消息序号；
- 每次消息创建都会更新会话的最近消息时间；
- 会话归属条件贯穿消息读写，不向其他用户暴露资源是否存在；
- 前端支持选择会话、查看历史消息和保存第一条经营问题；
- 当前阶段只保存用户输入，模型回复将在分析任务链路中接入。

### 2026-09-17：会话生命周期管理完成

- 实现会话重命名、归档、恢复和删除接口；
- 归档会话保持历史可读，但拒绝新增消息；
- 删除会话时依靠数据库外键级联清理消息及其他关联记录；
- 数据库事务提交后清理上传、导出和临时工作目录；
- 前端加入内联重命名、归档/恢复和带确认提示的删除操作；
- 自动化测试覆盖参数校验、归档限制、越权操作和删除后不可访问。

### 2026-09-17：工程规范审查与整改

- 按 `python-project-engineering` skill 审查现有依赖、配置、数据库和测试流程；
- 确认保留模拟认证依赖和 RESTful 会话接口；
- 将 `psycopg` 恢复为项目原先使用的 `3.2.9`；
- 移除仓库中的默认数据库密码、数据库连接和 Cookie 签名密钥；
- 本地真实配置只保存在 Git 忽略的 `.env` 文件中；
- 建立独立的 `insighttrace_test` 数据库和测试连接防误用检查；
- Alembic 在迁移前主动创建应用 schema，使新数据库可以独立完成迁移。

### 2026-09-17：会话附件管理完成

- 实现会话下的附件上传、列表、下载和删除 REST 接口；
- 支持 CSV、XLSX、JSON 和 TXT，限制单文件 20 MB、单会话累计 100 MB；
- 文件使用随机存储名并计算 SHA-256，数据库只保存受控存储根目录内的相对路径；
- 所有附件操作先校验当前用户对会话的归属，其他用户统一得到资源不存在响应；
- 归档会话可读取、下载和删除已有附件，但拒绝新增附件；
- 前端加入附件面板、上传状态、文件大小、解析状态、下载和删除操作；
- 附件解析尚未实现，新附件保持 `pending`，留待 M2 下一阶段处理；
- 后端 9 项测试、前端 2 项测试、Ruff 检查和前端生产构建通过。

### 2026-09-17：附件内容解析与 M2 收尾完成

- 上传完成后自动解析 CSV、XLSX、JSON 和 TXT，并写入现有 `parsed_content_json`；
- 解析状态依次进入 `parsing`，最终变为 `success` 或 `failed`；
- 解析失败不删除原文件，保存安全错误信息，并提供重新解析接口与前端操作；
- 对 CSV/XLSX 实施行列限制，对 XLSX 实施工作表限制，对 JSON 实施深度限制，对文本和单元格实施字符限制；
- CSV、JSON、TXT 使用 Python 标准库，XLSX 使用已确认的 openpyxl；Pandas 留到 M4 统计分析阶段再接入；
- M2 的登录、会话、消息、附件、资源隔离和主要工作台布局全部完成；
- 一次性 PostgreSQL 17 环境从零迁移成功，后端 13 项测试、Ruff 检查、前端 2 项测试和生产构建全部通过。

### 2026-09-17：M3 任务持久化与取消闭环完成

- 实现分析任务创建、单任务查询、会话任务列表、取消、重试和日志接口；
- 用户消息与分析任务在同一数据库事务中保存，创建失败不会残留孤立消息；
- 复用数据库部分唯一索引和会话行锁，保证同一会话只能有一个排队中或运行中的任务；
- 排队任务立即进入 `cancelled`，运行任务记录 `cancel_requested_at`，留给执行器协作式终止；
- 任务输入快照保存附件 ID 和消息 ID，重试任务通过 `retry_of_task_id` 关联原任务；
- 所有任务和日志查询都包含当前用户归属条件，其他用户统一得到资源不存在响应；
- 前端输入框改为创建分析任务，并显示最新状态、任务日志、取消和重新分析操作；
- 一次性 PostgreSQL 17 环境从零迁移成功，后端 15 项测试、Ruff 检查、前端 2 项测试和生产构建全部通过。

### 2026-09-17：M3 实时令牌与任务状态事件完成

- 实现 `POST /api/chat/ws-token`，只允许会话所属用户签发 60 秒有效的实时连接令牌；
- 数据库只保存令牌 SHA-256 摘要，不保存原始令牌，每个令牌成功连接后立即标记为已使用；
- 同一用户和会话重新签发令牌时废弃旧的未使用令牌，降低凭据泄露窗口；
- 实现 WebSocket 的 `connected`、`task_status`、`error` 和 `done` 事件；
- WebSocket 通过数据库任务版本发现变化，数据库仍是状态最终事实来源；
- 前端在活动任务出现时自动申请令牌、建立实时连接，并在事件到达后刷新任务和日志；
- 测试覆盖令牌一次性使用、过期拒绝、用户隔离和初始任务状态事件；
- 一次性 PostgreSQL 17 环境从零迁移成功，后端 17 项测试、Ruff 检查、前端 2 项测试和生产构建全部通过。

### 2026-09-17：M3 状态转换与重启恢复完成

- 建立受控状态转换服务，约束 `queued → running → success/failed/cancelled`；
- 当前步骤只允许使用技术设计中冻结的七个稳定步骤标识；
- 检测到取消请求后禁止进入下一步骤或保存成功结果；
- 成功结果、最终助手消息、任务日志和 `success` 状态在同一事务中提交；
- 运行中的任务完成协作式停止后进入 `cancelled` 并写入终止日志；
- 非测试环境启动时扫描遗留 `running` 任务，标记为 `WORKER_RESTARTED` 可重试失败；
- 测试覆盖成功原子提交、步骤日志、协作式取消完成和后端重启恢复；
- 一次性 PostgreSQL 17 环境从零迁移成功，后端 19 项测试和 Ruff 检查全部通过。

### 2026-09-17：M3 分析执行与实时事件闭环完成

- 实现单进程异步任务执行器，自动领取数据库中的排队任务，应用退出时进行有序停止；
- 执行器在每个稳定步骤检查协作式取消请求，异常任务保存安全错误并允许人工重试；
- 建立 Pydantic 结构化结果契约，以确定性演示分析验证任务编排、结果和助手消息闭环；
- 演示分析只总结当前数据源与缺失信息，不伪装成 M4 的经营归因能力；
- 持久化文本增量、工具开始、工具完成和结果就绪事件，断线重连后可从任务日志恢复；
- WebSocket 补齐 `message_start`、`message_delta`、`tool_start`、`tool_finish` 和 `result_ready`；
- 前端展示稳定步骤名称、实时执行动态和可读任务日志；
- 一次性 PostgreSQL 17 环境从零迁移成功，后端 22 项测试、Ruff 检查、前端 2 项测试和生产构建全部通过；
- M3 验收项全部完成，下一阶段进入 M4 的只读查询、指标计算与真实归因能力。

### 2026-09-17：M4 只读 SQL 安全层完成

- 实现只执行代码注册查询模板的异步 SQL 工具，模型和用户输入不能直接成为可执行 SQL；
- 查询模板必须通过单语句、SELECT/只读 CTE、危险关键字、危险函数和 schema.table 白名单校验；
- 查询使用绑定参数，外层统一施加最大返回行限制，并返回列、数据行、截断状态和查询名称；
- 每次执行使用独立 PostgreSQL 连接和只读事务，同时设置本地语句超时；
- SQL 超时和最大行数通过现有配置系统控制，默认分别为 15 秒和 2,000 行；
- 当前不引入第三方 SQL 解析依赖；如果未来允许模型生成任意 SQL，必须先补充 AST 解析器并重新评估安全边界；
- 独立 PostgreSQL 17 环境从零迁移成功，后端 31 项测试和 Ruff 检查全部通过。

### 2026-09-17：M4 附件内容读取与检索完成

- 实现基于 `attachment_id`、会话和当前用户三重归属校验的附件内容工具；
- 工具只读取数据库中已经通过 M2 安全解析的结构化内容，不接受文件路径，也不重新打开原始文件；
- CSV 和 XLSX 返回可追溯的行或工作表位置，TXT 返回行号，JSON 返回字段路径；
- 文本检索提供标准化关键词匹配、相关度排序、片段长度、扫描数量和返回数量限制；
- 不同用户访问同一附件时统一返回资源不存在，未解析成功的附件拒绝进入检索；
- 独立 PostgreSQL 17 环境从零迁移成功，后端 35 项测试和 Ruff 检查全部通过。

### 2026-09-17：M4 确定性指标计算完成

- 实现面向已解析表格和工作簿的确定性指标计算工具；
- 支持筛选、最多三维分组、计数、求和、平均、最小值、最大值和聚合比率；
- 数值计算使用 `Decimal`，支持千分位与百分号输入，最终通过结构化契约输出；
- 输出包含参与计算行数、跳过的无效行数、分组维度、指标单位和匹配行数；
- 缺失字段、空数据、零分母、过多数据行和过多分组均返回明确业务错误，不生成误导性指标。

### 2026-09-17：M4 分析上下文与历史摘要完成

- 实现当前问题、最近消息、较早历史摘要、上一次结构化结果和所选附件概览的统一上下文契约；
- 较早消息使用确定性摘要压缩，并按消息序号范围持久化复用，避免同一历史区间重复处理；
- 最近消息数量、单条消息长度和摘要总长度均有明确上限；
- 任务输入中的附件快照会重新验证存在性与解析状态，已删除或失败附件不会进入分析上下文；
- M3 执行器已在正式执行步骤中调用上下文组装服务，为后续真实分析提供统一输入；
- 独立 PostgreSQL 17 环境从零迁移成功，后端 41 项测试和 Ruff 检查全部通过。

### 2026-09-17：六部分结果、Markdown 导出与 M4 收尾完成

- 实现按任务读取问题定义、关键指标、证据、结论、缺失数据和下一步建议的结构化结果接口；
- 所有结果操作通过任务所属用户校验，其他用户不能读取、导出或下载结果；
- 前端新增六部分结果面板、指标卡、证据清单、置信度和报告操作；
- Markdown 报告保存到受控导出目录，使用临时文件和原子替换避免产生半成品；
- 数据库只保存存储根目录内的相对路径，下载前再次验证路径和文件存在性；
- 重复导出安全覆盖同一任务报告，会话删除时沿用现有机制清理整个导出目录；
- PostgreSQL 17 空数据库迁移成功，后端 42 项测试、Ruff 检查、前端 2 项测试和生产构建全部通过；
- M4 六项验收内容全部完成，项目下一阶段进入 M5 演示数据、脚本和最终验收。

### 2026-09-17：M5 商品目录真实数据闭环

- 新增 2026 年 7 月和 8 月可重复初始化的商品目录漏斗演示数据；
- 通过白名单只读 SQL 读取演示数据，不向分析器开放任意 SQL；
- 复用确定性指标工具计算整体、设备、类目、商品和渠道转化指标；
- 打通首条商品目录黄金问题，形成基于数据库事实的六部分结构化结果；
- 将版本变化标记为相关性，并明确需要性能和错误日志才能确认技术根因；
- 独立 PostgreSQL 17 环境迁移和回退均成功，后端 44 项测试与 Ruff 检查通过。

### 2026-09-18：M5 客户行为真实数据闭环

- 新增两个连续自然周的访问、浏览、加购、下单和可疑会话演示数据；
- 复用白名单只读 SQL 和确定性指标工具，拆解渠道、地区和新老访客差异；
- 打通“为什么本周访问到下单的转化率下降”黄金问题；
- 将可疑会话上升标记为相关性，不直接认定机器人流量、投放作弊或埋点故障；
- 独立 PostgreSQL 17 环境迁移和回退均成功，后端 48 项测试与 Ruff 检查通过。

### 2026-09-18：M5 多轮演示脚本完成

- 执行器开始使用已经组装的上一轮结构化分析上下文；
- 当前问题没有明确场景词时，依据上一轮证据的数据源继承商品目录或客户行为场景；
- 当前问题明确指定新场景时，以当前问题为准，不被旧会话场景覆盖；
- 新增两个场景各两轮的演示步骤、预期结果和验收观察点；
- 自动化测试覆盖场景继承、显式场景优先和无历史时不进行猜测。
- 独立 PostgreSQL 17 环境从零迁移成功，后端 54 项测试与 Ruff 检查通过。

### 2026-09-18：M5 接口与运维文档完成

- 基于实际 FastAPI 路由整理认证、会话、附件、任务、实时事件和结果接口；
- 记录 Cookie 认证、统一错误结构、资源隔离、任务状态和 WebSocket 令牌边界；
- 补充首次配置、启动、升级、停止、日志、测试、备份、恢复和完全重置操作；
- 在 README 建立项目规划、技术设计、API、运维和多轮演示文档入口；
- 文档相对链接、实际 OpenAPI 路径和 Docker Compose 配置检查通过。

## 16. 文档维护规则

后续每次方案讨论或范围调整后，应同步更新：

1. 文档顶部的“最近更新”日期；
2. 对应功能范围或非目标范围；
3. 里程碑任务状态；
4. “决策记录”中的状态和影响；
5. “规划进程日志”中的本次讨论摘要。

当某项提议得到确认时，将对应决策状态从 `提议` 更新为 `已确认`。如果后续推翻原决定，不直接删除记录，而是标记为 `已废弃`，并新增一条替代决策。
