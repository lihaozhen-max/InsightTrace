# InsightTrace MVP 多轮演示脚本

本文档用于在本地演示环境中稳定复现两个经营归因场景。每个场景应使用独立会话，
同一场景的第二轮问题必须在原会话中继续发送。

## 演示前准备

1. 启动 PostgreSQL、后端和前端服务；
2. 确认 Alembic 已升级到最新版本；
3. 使用模拟 OAuth 登录；
4. 打开“我的分析会话”。

## 场景一：商品目录优化

### 第一轮

创建会话“商品目录转化诊断”，发送：

> 为什么 2026 年 8 月商品目录的整体转化率较 7 月下降？

预期结果：

- 数据来源为 `demo_catalog.funnel_metrics`；
- 曝光量没有下降；
- 主要损耗位于移动端点击到加购环节；
- 星云手机是移动端加购率降幅最大的商品；
- 系统不会把应用版本变化直接认定为技术根因。

### 第二轮

在同一会话继续发送：

> 再具体一点，谁贡献最大？

预期结果：系统不要求重新说明月份、商品目录或指标口径，自动继承上一轮商品目录
场景，再次查询真实数据，并指出星云手机是优先排查对象。

## 场景二：客户行为分析

### 第一轮

创建会话“客户行为漏斗诊断”，发送：

> 为什么本周访问到下单的转化率下降？

预期结果：

- 数据来源为 `demo_behavior.funnel_metrics`；
- 访问量上升但订单量下降；
- 损耗主要集中在 `paid_social` 渠道的新访客；
- 华南地区受到的影响最大；
- 可疑会话占比明显上升，但系统不会直接断言机器人流量或投放作弊。

### 第二轮

在同一会话继续发送：

> 那最值得先检查哪一群人？

预期结果：系统自动继承上一轮客户行为场景和周度口径，并建议优先检查华南地区
`paid_social` 渠道的新访客及其可疑会话特征。

## 验收观察点

- 两轮任务都进入 `success`；
- 第二轮工具日志中的 `scenario_source` 为 `previous_analysis`；
- 页面能够看到工具开始、工具完成、结构化结果和 Markdown 报告；
- 第二轮问题和回答保存在原会话中，刷新页面后仍可恢复；
- 所有数字均来自只读查询和确定性指标计算。
